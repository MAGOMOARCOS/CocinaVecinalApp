name: Codex Agent

on:
  workflow_dispatch:
    inputs:
      instruction:
        description: "Si lo dejas vacío, usa AGENT_TASKS.md"
        required: false
        type: string
  push:
    branches: [main]
    paths:
      - "AGENT_TASKS.md"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: codex-agent-${{ github.ref }}
  cancel-in-progress: false

jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Create working branch
        shell: bash
        run: |
          BRANCH="agent/run-${GITHUB_RUN_ID}"
          echo "BRANCH=$BRANCH" >> $GITHUB_ENV
          git checkout -b "$BRANCH"

      - name: Prepare prompt
        shell: bash
        run: |
          if [ -n "${{ inputs.instruction }}" ]; then
            printf "%s\n" "${{ inputs.instruction }}" > .codex_prompt.md
          else
            cp AGENT_TASKS.md .codex_prompt.md
          fi

      # Ejecuta Codex vía GitHub Action oficial
      - name: Run Codex (apply changes)
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt-file: .codex_prompt.md
          output-file: codex-output.md
          sandbox: workspace-write
          codex-args: '["--full-auto"]'

      - name: Install deps + build (MUST pass before PR)
        shell: bash
        run: |
          set -e
          if [ -f package-lock.json ]; then
            npm ci || (echo "npm ci failed; running npm install to refresh lockfile" && npm install)
          else
            npm install
          fi
          npm run build

      # CLAVE: evita que el bot intente pushear cambios en workflows (GitHub los rechaza)
      - name: Protect workflow files (avoid push rejection)
        shell: bash
        run: |
          git restore --source=HEAD --staged --worktree .github/workflows || true

      - name: Cleanup agent artifacts
        shell: bash
        run: |
          rm -f .codex_prompt.md codex-output.md || true

      - name: Commit changes (if any) + push branch
        shell: bash
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes to commit."
            echo "PUSHED=false" >> $GITHUB_ENV
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "agent: apply AGENT_TASKS"
          git push --set-upstream origin "$BRANCH"
          echo "PUSHED=true" >> $GITHUB_ENV

      - name: Open PR
        if: env.PUSHED == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh pr view "$BRANCH" --json number >/dev/null 2>&1; then
            echo "PR already exists for $BRANCH"
            exit 0
          fi

          gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "agent: apply AGENT_TASKS" \
            --body "Automated changes from Codex Agent. Build passed in workflow run."
