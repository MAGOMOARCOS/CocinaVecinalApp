name: Agent Setup

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Agent Task
        run: |
          echo "Agent starting..."
          # Aquí podrá ejecutar tareas automáticas
          # como crear /app, /lib, /components, etc.
